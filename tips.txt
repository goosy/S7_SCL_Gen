GCL配置文件API有不兼容改变，以下改变不兼容以前的配置文件：

所有FB块的输入参数，初始值参数必须有$前缀。

以AI的条目为例

- comment: 进站温度
  DB: [TIT001, DB+] 
  input: [AI01-03, PIW516] # 
  init_enable_alarm: 'pump1.run_state' # 以上2项为运行期间赋值，运行期间只能由配置值修改
  $zero: "-40.0"
  $span: "80.0"
  $AH_limit: "2.5"
  $WH_limit: "2.0"
  $WL_limit: "-0.05"
  $AL_limit: "-0.1"        # 以上7项为初始赋值，可在运行期间由外部修改
